server {
	error_log debug;

	listen 8080;
	server_name uploads.<%= app_domain %>;

	root /home/<%= app_name %>/uploads.<%= app_domain %>;

	location / {
		try_files $uri $uri/ @s3;
		expires 1m;
		add_header Pragma public;
		add_header Cache-Control "public, must-revalidate, proxy-revalidate";
	}

	# pass to S3 if not found
	location @s3 {
		proxy_cache_methods GET HEAD;
		proxy_pass_request_body off;
		#proxy_pass_request_headers off;
		proxy_set_header Content-Length 0; # prevent big POST-style requests to S3
		proxy_pass http://media.<%= app_domain %>; # trailing slash is important
	}
}